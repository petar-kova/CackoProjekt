# CackoProjekt

ASP.NET Core MVC web projekt sa Razor view-ovima i ViewModel klasama.

## Pokretanje

```bash
dotnet restore
dotnet build
dotnet run
```

Ako dobiješ `NETSDK1004` grešku, prvo odradi `dotnet restore`.
